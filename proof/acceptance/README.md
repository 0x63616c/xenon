# Full acceptance profile — not executed

`full-profile.json` is the next workload profile for the existing ministack controller, not another launcher. It preserves the budgets in `docs/design/acceptance.md`. `python3 scripts/validate_acceptance.py` verifies the frozen profile and saved corpus bindings on a clean checkout. `python3 -m unittest discover -s scripts -p 'test_acceptance_profile.py'` checks that changed counts, removed correctness assertions, invented runtime success and changed replay inputs fail. Updating the fingerprint requires a prospective reviewed contract change; changing a budget after failure cannot produce a pass for that failed run.

Pinned Omes `scenarios/throughput_stress.go` declares `internal-iterations` and `continue-as-new-after-iterations` as integer options, `include-retry-scenarios` and `include-describe` as booleans, and `sleep-time`/`visibility-count-timeout` as durations. `cmd/omes/run_scenario.go` declares iterations, max-concurrent and timeout. The committed command uses their exact spellings and the mandated values. No claim is made that throughput_stress automatically covers every required history event: capability discovery and actual history coverage are separate mandatory assertions. Feature-gated standalone actions follow actual public capability discovery and must be reported.

The existing controller should consume this profile after its current smoke passes. First wire public SDK compatibility and both Temporal service counters; then execute the100simple and40mixed iterations while collecting complete workflow/run chains and operation latency samples. Scale at the declared completed-operation barrier while work remains active. Perform both domain moves and compare incarnation-bound local dispatch counters, not recovered journal totals. Seed the separate frozen2000-record visibility fixture and verify exact lists/count/groups using all three page sizes before/during/after movement; concurrent mutation has separate assertions.

Replay each of the20immutable protobuf inputs using the existing saved command manifest in separate no-fault/fault environments. Preserve the Nexus endpoint setup from that manifest. Each fault identifier must be bound to a real observed event hook, independently logged trigger and post-fault oracle before the controller can execute this profile. The current document does not implement those hooks. A missing hook is a blocked/incomplete run, never a skipped passing test. Fault and steady windows are declared in the profile; samples include retries, and each mandatory per-family p99 needs at least100 observations. Enumerate every acknowledgment and reconcile ambiguous outcomes against recoverable state.

Wire the same input profile into the real-S3 overlay with only the four listed storage settings changed. The final controller must capture the enclosing immutable image/tool/environment manifests and scoped cleanup evidence. Profile validation proves input integrity only; actual workflows, faults, UI, recovery, metrics and real S3 remain runtime gates. This work does not replace or reduce the existing smoke profile.
