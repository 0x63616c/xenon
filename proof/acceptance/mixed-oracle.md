# Mixed semantic history oracle

Source-derived from Omes c6978ba39aa03551ce28974117e8d7ecf983d2b3: `scenarios/throughput_stress.go` createActionsChunk/createChildWorkflowAction/createSelfUpdate* and completed-workflow formula; `workers/go/workerlib/kitchensink/kitchen_sink.go` RetryableError/Timeout/Heartbeat; `clioptions/client.go` OmesDataConverter/PassThroughPayloadConverter. Temporal 19a774302c613da9adc4436ab14278ccdca8e0a5 records actual attempt in historybuilder/event_factory.go CreateActivityTaskStartedEvent. Intermediate retry failures are not required history events.

After the frozen mixed40 workload and visibility convergence, build `go build -o .local/bin/xenon-omes-oracle ./cmd/xenon-omes-oracle` and execute `.local/bin/xenon-omes-oracle --omes-run-id xenon-full-mixed --mixed-profile --output <new-evidence-directory>`. The existing public API capture reads every visible history page, retains exact protobuf JSON bytes and hashes, validates contiguous event IDs and terminal state, and checks both Continue-As-New directions. Each RPC retains30s and the overall capture15min cap. This audit budget does not extend the workload900s budget.

The baseline oracle requires40 roots of one Omes execution, each with two runs, and160 referenced children. Parent initiation/start/completion identities must agree with the actual child's reverse parent identity. Each child has three completed payload activities. Each parent run requires two signals, four fired timers, two completed retryable_error/timeout/heartbeat activities each with final attempt>=2, and two completed updates for each timer/remote256/local256 variant. An optional empty Update-with-Start is allowed. Completed workflow results must exactly match Omes' empty protobuf bytes wrapped with `_passthrough` encoding.

`python3 scripts/prove.py mixed-oracle` executes the committed deterministic240-history recipe and negative controls. It does not query a live server. Runtime execution is pending. The visible set is not an independent census of all attempted starts; the controller must still reconcile an independently captured start inventory. Update result/state payloads and activity random payload contents are retained but not semantically reconstructed. This is not full acceptance, fault execution, or proof that every arbitrary fuzz branch ran.


The original exact240 TaskQueue capture was incorrect for the frozen Nexus-enabled profile. The capture now retains **all** queue-visible runs and rejects `--exact-runs` with mixed mode. Baseline240 and Nexus480 are validated separately; unknown runs cannot disappear from the captured set or pass classification. Nexus-enabled pinned throughput_stress creates, per internal iteration, one synchronous echo (no handler), one asynchronous echo handler, one canceled handler, and one attach handler shared by three successful operations. Thus each parent run has12 finished Nexus operations and6 distinct handlers; all80 parent runs yield480 handlers, for720 visible workflow runs total.

Each async start's SDK1.48 operation token (type1, namespace, workflow ID) and persisted HistoryEvent link must identify the same captured handler run. Echo results are exactly JSON string `hello`; cancellation requires the parent cancel request, canceled operation and canceled handler terminal event. The three attach operations must reference the same handler in one parent run, all complete, and match its exact `unblock` signal. Handler input variants are validated against pinned `createNexus*Action`; no unrelated canceled workflow is accepted. The public capture's contiguous-history check remains mandatory.

This is specific to Temporal1.31.2: `service/frontend/namespace_handler.go` does not advertise the later `StandaloneNexusOperation` capability, so pinned Omes does not enable standalone Nexus actions by default. It is not a command-line feature suppression. A future server that advertises it requires a new oracle: direct standalone client operations do not necessarily preserve a parent workflow link. Unknown additional runs currently fail rather than being mislabeled. Standalone activities use their separate division and do not add default workflow-visible runs.

Source evidence: Omes c6978ba `scenarios/throughput_stress.go` createActionsChunk/createNexus*Action, `loadgen/scenario.go` NexusEndpointForRun, `workers/go/workerlib/kitchensink/kitchen_sink.go` NexusHandlerWorkflow/EchoAsyncOperation and `clioptions/client.go` converter ordering. SDK1.48.0 `temporalnexus/operation.go` supplies caller task queue, exact workflow links and tokens; `temporalnexus/token.go` pins token encoding; `internal/internal_nexus_task_handler.go` uses the configured payload converter. Temporal19a774 `service/frontend/namespace_handler.go` pins advertised capabilities. The timer/update fixture now uses the actual proto JSON `milliseconds`/`remote` fields.

The three registered commands execute only synthetic720-history controls and the baseline recipe. They do not establish a live mixed result, an all-started census or arbitrary Nexus/standalone compatibility. The runtime must still execute the frozen workload and retain the full capture before claiming this component passed.

The ministack `--omes-mixed` controller builds and invokes this oracle after the unchanged 900-second workload deadline. The existing 15-minute history-audit budget is separate.
