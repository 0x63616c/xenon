diff --git a/workers/proto/kitchen_sink/kitchen_sink.proto b/workers/proto/kitchen_sink/kitchen_sink.proto
index 39a2927..3a90555 100644
--- a/workers/proto/kitchen_sink/kitchen_sink.proto
+++ b/workers/proto/kitchen_sink/kitchen_sink.proto
@@ -176,6 +176,9 @@ message WorkflowInput {
   // Signal de-duplication state (used when continuing as new). Signal IDs use 1-based indexing.
   repeated int32 expected_signal_ids = 3;
   repeated int32 received_signal_ids = 4;
+
+  // Xenon compatibility overlay: allowed once, without a completion dependency.
+  repeated int32 optional_signal_ids = 5;
 }
 
 // A set of actions to execute concurrently or sequentially. It is necessary to be able to represent
diff --git a/workers/go/workerlib/kitchensink/kitchen_sink.go b/workers/go/workerlib/kitchensink/kitchen_sink.go
index 873e0be..0e3d806 100644
--- a/workers/go/workerlib/kitchensink/kitchen_sink.go
+++ b/workers/go/workerlib/kitchensink/kitchen_sink.go
@@ -238,7 +238,7 @@ func isSignalAlreadyReceived(params *kitchensink.WorkflowInput, signalID int32)
 		return true
 	}
 	// If the signal ID is not in the expected list, it means we already processed it
-	return !slices.Contains(params.ExpectedSignalIds, signalID)
+	return !slices.Contains(params.ExpectedSignalIds, signalID) && !slices.Contains(params.OptionalSignalIds, signalID)
 }
 
 // handleSignalDeduplication removes a received signal ID from the expected list.
@@ -246,6 +246,13 @@ func handleSignalDeduplication(params *kitchensink.WorkflowInput, signalID int32
 	if params == nil {
 		return
 	}
+	// Optional IDs are a strict whitelist, consumed once without delaying return.
+	for i, id := range params.OptionalSignalIds {
+		if id == signalID {
+			params.OptionalSignalIds = append(params.OptionalSignalIds[:i], params.OptionalSignalIds[i+1:]...)
+			return
+		}
+	}
 	// Remove the signal ID from the expected slice
 	for i, id := range params.ExpectedSignalIds {
 		if id == signalID {
diff --git a/workers/go/workerlib/kitchensink/kitchen_sink.go b/workers/go/workerlib/kitchensink/kitchen_sink.go
--- a/workers/go/workerlib/kitchensink/kitchen_sink.go
+++ b/workers/go/workerlib/kitchensink/kitchen_sink.go
@@ -37,6 +37,7 @@
 		},
 		WorkflowType:  "kitchenSink",
 		WorkflowInput: &kitchensink.WorkflowInput{},
+		AllowEmptySignalWithStartFallback: true,
 	}
 	return executor.ExecuteClientSequence(ctx, clientActivity.ClientSequence)
 }
diff --git a/loadgen/kitchensink/client_action_executor.go b/loadgen/kitchensink/client_action_executor.go
--- a/loadgen/kitchensink/client_action_executor.go
+++ b/loadgen/kitchensink/client_action_executor.go
@@ -11,9 +11,12 @@
 	"go.temporal.io/sdk/client"
 	"go.temporal.io/sdk/workflow"
 	"golang.org/x/sync/errgroup"
+ "google.golang.org/protobuf/proto"
 )
 
 type ClientActionsExecutor struct {
+ // Only the Go client activity uses an empty fallback for a late SignalWithStart.
+ AllowEmptySignalWithStartFallback bool
 	Client          client.Client
 	Namespace       string
 	WorkflowOptions client.StartWorkflowOptions
@@ -29,7 +32,7 @@
 ) error {
 	var err error
 	if withStartAction == nil {
-		e.Handle, err = e.Client.ExecuteWorkflow(ctx, e.WorkflowOptions, e.WorkflowType, e.WorkflowInput)
+		e.Handle, err = e.Client.ExecuteWorkflow(ctx, e.WorkflowOptions, e.WorkflowType, input)
 	} else if sig := withStartAction.GetDoSignal(); sig != nil {
 		e.Handle, err = e.executeSignalAction(ctx, sig)
 	} else if upd := withStartAction.GetDoUpdate(); upd != nil {
@@ -154,7 +157,16 @@
 	}
 
 	if sig.WithStart {
-		return e.Client.SignalWithStartWorkflow(
+        input := e.WorkflowInput
+        // Existing executions ignore this start argument. For a new execution,
+        // admit only this signal, without changing its actions or completion rules.
+        if e.AllowEmptySignalWithStartFallback && input != nil && proto.Equal(input, &WorkflowInput{}) {
+            if actions := sig.GetDoSignalActions(); actions != nil && actions.SignalId > 0 {
+                input = proto.Clone(input).(*WorkflowInput)
+                input.OptionalSignalIds = []int32{actions.SignalId}
+            }
+        }
+        return e.Client.SignalWithStartWorkflow(
 			ctx, e.WorkflowOptions.ID, signalName, signalArgs, e.WorkflowOptions, e.WorkflowType, e.WorkflowInput)
 	}
 	return nil, e.Client.SignalWorkflow(ctx, e.WorkflowOptions.ID, "", signalName, signalArgs)
